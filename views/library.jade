extends layout

block head   
block content  
  .container
    include includes/navbar
    .contaier
    include includes/filterPanel
      div(ng-app="snippetsLib")
        div(ng-controller="SnippetsCtrl")
          a.col-sm-6.col-xs-6.col-md-6.selectable(ng-repeat="snippet in Snippets")
            .thumbnail
              .ribbon--corner--nw.ribbon--green
                .ribbon New
              .badge.pull-right.count 8
              img(src="http://placehold.it/525x125" alt="...")
              .caption
                span.label.label-primary {{snippet.tags}}
                span.text-muted.pull-right {{snippet.artist}}
    //- ul.pagination.pull-right
    //-   li.selectable(ng-hide="pagination.page == 0" ng-click="pagination.prevPage()")
    //-     a « Prev
    //-   //- span(ng-repeat="n in [] | range: pagination.numPages")
    //-   //-   button(ng-click="pagination.toPageId(n)" {{n + 1}})
    //-   li.selectable(ng-hide="pagination.page + 1 >= pagination.numPages" ng-click="pagination.nextPage()") 
    //-    a Next »

    //- | startFrom: pagination.page * pagination.perPage | limitTo: pagination.perPage